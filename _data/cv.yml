# CV content is driven by assets/json/resume.json (cv_format: jsonresume in _pages/cv.md).
# This file is intentionally left empty so the legacy RenderCV loader does not
# pick up demo data. The downloadable PDF lives at /assets/pdf/cv.pdf.
